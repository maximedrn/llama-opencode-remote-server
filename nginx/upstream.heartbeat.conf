# `--keepalive`: the clients reach the keep-alive front, which relays to
# llama.cpp and holds the connection open while it is silent.
upstream llama_upstream {
    server heartbeat:8081;
    keepalive 32;
}
